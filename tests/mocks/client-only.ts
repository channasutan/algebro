/**
 * Mock for client-only package in test environment.
 * 
 * In tests, we allow importing client-only modules to verify their behavior.
 * The actual build-time protection is enforced by Next.js bundler in production.
 */
export {};
