{
  "version": 1,
  "directives": [
    {
      "name": "Infrastructure must not depend on modules",
      "constraint": "infrastructure -> modules",
      "action": "forbid"
    },
    {
      "name": "Events must not depend on modules",
      "constraint": "events -> modules",
      "action": "forbid"
    },
    {
      "name": "Config must not depend on modules",
      "constraint": "config -> modules",
      "action": "forbid"
    },
    {
      "name": "Modules isolation",
      "constraint": "modules -> modules",
      "action": "forbid"
    }
  ]
}